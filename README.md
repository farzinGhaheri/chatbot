For production clone the restructure repository and build locally => 'npm install' -> 'npm run build' then deploy 'dist' + 'package.json' + '.env' to the server, 

install deps => 'npm install' and run 'node dist/server.js'
